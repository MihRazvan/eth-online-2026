# usufruct status

Updated 2026-09-12. Integration owner: Codex lead. Remote: MihRazvan/eth-online-2026, main. Remote was verified empty; no rulesets. Local folder originally held only the handoff.

Current direction: **usufruct**, with the implemented orchard identity in [DESIGN.md](../DESIGN.md), is now preparing for ETHOnline 2026 **Classic**, explicitly confirmed by the user. The local folder name is accidental. This supersedes the earlier instruction to defer hackathon work. The [judge-readiness checklist](JUDGE_READINESS.md) now owns release tasks and team dependencies; the [bounty readiness research](research/bounty-readiness.md) supplies architectural rationale. The release prioritizes operated endpoint preservation, a complete public lifecycle, bounded offer terms and live Graph composition. Uniswap and 1inch are the primary targets; The Graph remains conditional on real provider integration. Deadline: 13 September 2026, 19:00 Bucharest. The earlier [product research](research/README.md) remains supporting context.

| Work | Owner | State | Evidence / next action |
| --- | --- | --- | --- |
| Classic bounty readiness | Lead / separate protocol and frontend reviewers | assessment complete; implementation gates open | [Decision memo](research/bounty-readiness.md), [sponsor matrix](evidence/sponsors.md); no new transactions or hosted services in this assessment |
| Baseline / capability check | Lead | verified | GitHub write authentication; Node 24.12.0, pnpm 12.3.4, Foundry 1.5.1, Slither 0.11.5; baseline packet check |
| Contract lifecycle and accounting | Protocol specialist | local lifecycle verified | Native canonical collection + funded custody + independent real-proof lifecycle; dynamic4-NFT lifecycle and custody bypasses verified |
| Historical endpoint | Proof specialist | component verified | Genuine Sepolia witness + fork BLOCKHASH + native collection match; full local real-proof lifecycle verified; controlled public lifecycle pending; tip-only public proof retention |
| usufruct identity / application | Frontend specialist / lead | integrated and locally verified | User-directed orchard identity; 38 UI regressions; desktop/mobile review; both real-chain browser recovery variants; public-data preview |
| Aqua/SwapVM | Lead | local verified | Actual escrow-issued FeeClaim/USDC transfer;11 market tests; callback finding fixed and independently reviewed |
| Graph composition | Lead | Both sources live; buyer join pending | Studio v0.2.0 and authenticated Substreams v0.1.1 verified. Seeded history, bounded HTTP composition and container child are locally verified; hosted operation and actual series analysis remain pending; [evidence](evidence/graph-service.md) |
| Public endpoint operations | Lead | not operated end to end | Restricted keeper, remote restoration and HTTPS gateway implemented; operate the reviewed service/bucket and verify preservation before activating a public sale |
| Independent economic / browser review | Separate reviewers / lead | increment reviewed | Callback correction verified by second reviewer; actual rendered/browser financial-label fixes; integration-review.md |
| Product research / recovery | Lead / research specialists | research complete; first increments verified | Cited research synthesis; buyer cancellation, authenticated durable retention/API, shared entitlement receipt, bid execution and maker recovery verified locally; recovery UI and exact onchain cache path integrated |

Full financial acceptance gates in the product contract remain pending; component results above have their stated limits. Prior archive tests are inherited component evidence only. The six FeeStrip/Aqua contracts are deployed on Sepolia and canonical NFT39216 is owned by the configured wallet. Public sale/settlement acceptance remains pending. Complete local chain lifecycle passed: nativeN799999999, capture1199999999, residual400000000, final dust2 USDC base units; evidence local-lifecycle.json.

## Access gaps

Project Sepolia RPC and a local deployment key are now configured in ignored `.env`. Read-only qualification confirmed chain11155111, canonical deployment/code pins, and independently authenticated historical account/storage proofs at sampled offsets1,128,8191; a transient tip failure passed on recheck. The configured wallet received20 native test USDC. Public setup used4.95USDC for liquidity, leaving15.05USDC;0.003testETH was wrapped and deployment gas cost0.014009397442426351testETH. Public deployment has since completed. Studio access is configured and its Sepolia Subgraph is deployed; Graph Market/Substreams access is also verified; participant-wallet acceptance remains pending. No transaction was broadcast during qualification. [Sanitized RPC evidence](evidence/project-rpc-qualification.json). Never substitute trusted-server settlement for missing proof access. Do not log secrets.

## Execution

One ledger, native agents, isolated worktrees. Lead owns shared ABI, root files and dependency lockfiles. Each coherent verified increment is committed and pushed after fetch/reconciliation. No mainnet spending. User explicitly authorized implementation, routine decisions, specialist agents, commits and pushes.

Reproducibility: docs/development.md. Static analysis reproduces13 explicitly reviewed findings, not a clean report/audit. Incorrect-denominator mutation is killed in a disposable project. Independent data review corrected mixed-fork snapshot attribution, Graph collection naming, checkpoint provenance and closed-series purchase metrics; see data-review.md. Hosted CI is green for implementation `6ce74e1`: [run34609205562](https://github.com/MihRazvan/eth-online-2026/actions/runs/34609205562), all three jobs successful. This includes47contract tests, reviewed static findings, mutation kill,17core/data tests,14fixture/scope browser tests, the complete real-chain browser lifecycle, Subgraph build and5Rust/12WASM-sink tests. Counts overlap across shared sink suites and are not an aggregate unique-test claim. The initial browser job exposed the documented same-URL login harness issue; the corrected helper passes both independent local and hosted runs. Full public/live financial acceptance remains blocked by the access gaps above.

Follow-up check: latest published documentation commit `78bb15e` also passed all hosted jobs: [run34609641305](https://github.com/MihRazvan/eth-online-2026/actions/runs/34609641305). Submission checklist reconciled with completed browser and reusable Substreams work; live/provider and human gates remain open.

Research increment: `pnpm test` passes27tests (17existing core/data +10witness acquisition), `pnpm build` passes,16fixture/scope browser tests pass, and the complete real-chain browser suite passes with EIP-1898 witness acquisition and cancellation of the100test-USDC seed offer. A separate Anvil node at8546 isolated this run from8545. Independent reviewers checked acquisition and refund paths; browser integration caught and fixed decimal-string chain metadata compatibility. Evidence and remaining validation limits: [research implementation review](evidence/research-implementation.md). Hosted verification passed all three jobs for research/recovery commit `8704827`: [run34620553425](https://github.com/MihRazvan/eth-online-2026/actions/runs/34620553425), including the fresh-checkout actual-chain browser lifecycle, contract/static/mutation checks and Substreams suite.

Durability and quote increment: independent validation passes13Python cryptographic tests plus36Node settlement/retention tests. Root reran27core/data/acquisition tests,26fixture/receipt/quote/wallet-safety tests, production build and the complete actual-chain browser lifecycle. Retention exercises scheduled N acquisition, offline restart, partial writes, corruption in either storage layer, reorg variants, finality regressions, bounded due-job priority, exact-byte downloads and malformed HTTP requests. Separate reviewers verified the storage corrections and the immutable reviewed-signer fix. Real native collection remains799999999, captured1199999999, residual400000000 and dust2 USDC base units; the actual lifecycle now includes worker retention and a validated recovery API. [Operations and limits](../packages/settlement/README.md), [quote evidence](design/quote-controls.md), [receipt evidence](design/entitlement-receipt.md). Hosted CI passed all three jobs for `1c957c3`: [run34624850276](https://github.com/MihRazvan/eth-online-2026/actions/runs/34624850276), including actual bid/cancellation transactions and independent native collection with retained proof recovery. No new public acceptance is claimed.

The first human session protocol is ready in [the user-test plan](user-test-plan.md), with separate seller, buyer and maker tasks, unaided whole-period/original-Q comprehension prompts, recovery exercises and evidence capture. It distinguishes local unlocked rehearsal, public injected-wallet acceptance and live Graph integration.

Recovery UI integration:33fixture/safety/receipt/quote/recovery tests and production build pass. The actual browser lifecycle now uses the real recovery service, validates a downloaded proof digest, and allocates with the static witness removed. A separate run caches authenticated growth, removes every retained proof copy, then allocates from a direct verified onchain cache read. Both preserve NFT return before allocation, original-Q holder payouts, residual accounting and the expired-offer refund. Hosted CI passed all three jobs for the completed implementation `1ceb2ba`: [run34626081991](https://github.com/MihRazvan/eth-online-2026/actions/runs/34626081991). This includes both real-service/cache browser variants, the native collection/retention lifecycle, actual bid/cancellation transactions,33UI tests,36Node settlement tests,13Python cryptographic tests,27core/data/acquisition tests, contract/static/mutation checks and Substreams verification. These counts describe their separate suites and are not an aggregate unique-test claim. [Integrated recovery evidence](design/recovery-controls.md#integrated-service-and-contract-evidence).

Public setup preparation: the bounded canonical mint and six-contract deployment runner passed a dedicated Sepolia fork rehearsal, exact receipt replay and native custody/capture/NFT-return compatibility check. Independent review corrections are documented in [the setup review](evidence/sepolia-setup-review.md); [runbook](../scripts/public/README.md). Nine offline setup tests,36UI tests, production build and the complete local browser transaction lifecycle pass. No public sale or human wallet acceptance is implied by this rehearsal.

Public checkpoint: all12 setup transactions confirmed; [manifest](../deployments/sepolia.json), [deployment evidence](evidence/sepolia-deployment.json), [independent NFT/balance readback](evidence/sepolia-position-readback.json). A separate specialist verified all6 runtime hashes and15 immutable bindings through PublicNode. NFT39216 remains in the original wallet with fixed minted liquidity; no offer or sale was activated. The public-data production preview at http://127.0.0.1:4187 passes disconnected desktop/mobile checks and rejects test-wallet injection. Recovery service8788 observes the public deployment with0series; it does not sign checkpoints. Private config stays under ignored .scratch. [Browser scope](evidence/sepolia-browser-smoke.json). The full hosted suite passed implementation fdf8e40: [run34642650161](https://github.com/MihRazvan/eth-online-2026/actions/runs/34642650161). The subsequent empty-market wording and private-config helper pass local validation; public participant/endpoint-proof and live Graph gates remain open.

Branding increment verified: **usufruct** now has the requested paper/orchard identity, licensed self-hosted Instrument Serif and Courier Prime, reusable SVG marks, a brand folio, and separate Orchard / Pin a tree / My cabinet workflows. Independent economic and rendered reviews informed the implementation; root fixed post-acceptance navigation hiding the confirmation receipt. All 38 UI regressions and both actual local-chain browser variants pass, including retained proof recovery and verified-cache recovery. Original-Q payouts, capture/NFT-return ordering and the exact expired-offer refund remain unchanged. TypeScript and the testnet production build pass. The public-data preview at http://127.0.0.1:4187 and explicit fixture design preview at http://127.0.0.1:4192 serve the new identity; `/brand/index.html` contains the identity folio. [Design and evidence](design/usufruct-implementation.md). Public participant-wallet acceptance and live Graph integration remain pending; no public transaction was made for this design increment.

Vercel frontend published on 12 September 2026: https://usufruct-mu.vercel.app, project `usufruct` in `mihrazvans-projects`. The authenticated CLI created and deployed the project, then connected the GitHub repository for automatic deployment. The cloud build and unauthenticated desktop/mobile browser checks pass; the site uses Sepolia with no fixture wallet or simulated market data. `pnpm build:vercel` isolates public output from local manifests, witnesses and environment files and is included in CI. [Deployment runbook](deployment-vercel.md), [browser evidence](evidence/vercel/browser-smoke.json). Proof retention and Graph analytics still require separately hosted production services. No wallet transaction was sent during deployment.

Wallet discovery correction,12September: eligible participant NFTs39220/39221/39222 were absent because discovery only used configured/application-known IDs. The app now searches recent canonical incoming wallet transfers, revalidates ownership/eligibility, and offers a read-only NFT ID/Uniswap-link lookup for older/missed positions. No manifest IDs or contract restrictions changed. All47UI tests, TypeScript and the isolated Vercel build pass; a live-RPC read-only browser check found all three positions at desktop/mobile widths. Separate review caught and verified fixes for discovered-token metadata failures and exact-cap truncation. [Evidence and limits](evidence/position-discovery.md). No participant signature, public sale or transaction occurred.

Judge-readiness pass: [one release checklist](JUDGE_READINESS.md) now maps lead/team tasks to exact acceptance and partner evidence. Read-only live inspection confirmed empty markets and disconnected recovery/analysis APIs. Separate reviewers identified the remaining external-buyer target lookup, refresh/signature continuity, native-activity compatibility and quote-renewal-after-redemption gates. Presentation improvements add a first-visit guide, public Network & data label and Source & credits footer; ScopeLift attribution remains in repository documentation. TypeScript, isolated public build and all47UI tests pass; keyboard/desktop390px-mobile review passes. No financial action or service deployment is claimed by this increment.

Graph Studio checkpoint,12September: `usufruct` v0.1.0 is deployed against the actual FeeStrip address from block11684159. The indexer advanced from11688842 to11688863; a hash-pinned query matched PublicNode and was one block behind the sampled RPC head, with no indexing errors. Empty series/event lists agree with onchain `nextSeriesId=1`. This verifies live queries, not sale-event mapping or the live Substreams join. Public deployment configuration, secret-isolating deployment helper and read-only verification are committed; [evidence and next access step](evidence/graph.md). Railway CLI authentication is already available; project hosting, paid-plan selection and operator setup have not been completed.

Graph Market checkpoint,12September: the supplied JWT authenticated live Substreams. Qualification found and fixed two integration issues: gRPC omitted terminal status (binary Connect completes cleanly), and upstream transaction-local log indices needed normalization through full-block receipts. The reusable map is nowv0.1.1; existing databases must resync under its new package hash. Actual NFT39216 mint output agrees with RPC at global log107, two bounded runs resume correctly across20 blocks, and a separate20-block range matches RPC/Studio at11688819. Eight Rust tests,13WASM/sink/source tests and29core/data/acquisition tests pass. Credentials and raw cursors stay out of public evidence. [Qualification and limits](evidence/substreams.md). Both Graph access steps are complete; continuous hosting, initialized range history, actual sale mapping and joined buyer analysis are still release gates.

Checklist reconciliation after7bb04bb: [the next implementation batch](JUDGE_READINESS.md#current-checkpoint-and-next-implementation-batch) is restricted checkpoint/recovery operation (J09/J10) alongside the two-wallet offer flow and prerequisites (J04–J08/J14). At block11689036 there were still no activated series; public recovery/analysis routes returned404 and Railway had no linked project. Both Graph access steps are complete; J15/J17 retain their remaining initialized-history/hosted-composition scope. [Hosted CI for7bb04bb](https://github.com/MihRazvan/eth-online-2026/actions/runs/34694845577) has passed contracts and Substreams; application was still running when checked. The preceding Studio commit50d4df5 passed all jobs. No additional credential, spend approval, wallet signature or external submission was requested for this planning checkpoint.

Endpoint/offer implementation checkpoint,12September: restricted keeper, S3-compatible authenticated off-host proof recovery, container/API deployment bundle and two-wallet offer UX are implemented. Independent reviews corrected post-signature gas/budget readiness, receipt-time budget accounting, pending nonce exclusivity, shared-endpoint remote lookup and frontend duplicate-submission/replacement handling. A material pre-existing fundOffer liquidity-substitution race now has an onchain reviewed-position commitment guard and adversarial regression; the eight-argument ABI requires a new immutable public deployment. Current root checks:51Solidity tests,29core/data/acquisition tests,46settlement Node tests plus13Python cryptographic tests,14keeper units and one actual isolated-Anvil checkpoint/reorg test,6operations tests,57browser regressions, public frontend/Subgraph builds, original-Q mutation kill and13unchanged reviewed static findings. Both actual local browser variants passed the new contract, including an unknown NFT and separate buyer/seller. Counts cover separate suites and overlap; not an aggregate claim.

The built container authenticated a fresh Sepolia proof read-only; it correctly refused new sales without signer/bucket readiness. Railway's empty isolatedusufruct project and a three-resource plan are prepared, with no billable resource provisioned. [Deployment plan](deployment-operations.md). At public block11689194, there were0activatedseries and672testUSDC in unaccepted offer2; the user is coordinating the buyer's refund. Refund + replacement FeeStrip/market deployment were rehearsed on a separate fork at11689200 (5,196,051deployment gas), not broadcast publicly. Public deployment switch, hosted checkpoint/bucket rehearsal, live Graph join and human financial acceptance remain blocked on their explicit checklist gates.

Buyer refund verified onchain: offer2 was cancelled by the participant at block11689226, returning672SepoliaUSDC. Fresh readback at11689275 confirms consumed=true, zero old funded liabilities, zero old contract USDC and no activated series. [Receipt evidence](evidence/operations/old-offer-refund.json). Replacement deployment is now unblocked; hosted preservation and public lifecycle gates remain open.

Receipt reorg follow-up:61hermetic browser regressions, TypeScript and isolated Vercel build pass. Separate review verified public replacement finality, unresolved history retention and conservative authorization-transaction classification. The actual local browser cancels/reconciles, orphans that cancellation block, then verifies zero new transaction RPCs and restored pending guards in both recovery configurations. This is a browser-local guard under Ethereum-finality/RPC/storage assumptions, not cross-device idempotency. The preceding complete implementation8a873b7 passed all three [hosted CI jobs](https://github.com/MihRazvan/eth-online-2026/actions/runs/34697434807). Its production frontend/API smoke confirms desktop/mobile and503fail-closed responses; [evidence](evidence/operations/vercel-smoke.json). Paid hosting remains unprovisioned pending a decision after the user asked why it is needed.

Public commitment upgrade completed: FeeStrip0xd84eE56b24841c887c73e8b9079A01c28b1B3E8E at11689365 and market0x08B350FB5f2a1efF499F5cECD8C6Df427F282882 at11689367. Independent PublicNode readback checks reviewed runtime/source,22bindings, canonical position commitment and zero old/new liabilities; receipts had17/15confirmations, not finality, at11689381. [Evidence](evidence/operations/commitment-upgrade-readback.json). Total5,196,051gas /0.005672363800718797SepoliaETH. The archived old manifests/evidence preserve the refunded deployment. Studio v0.2.0 matches the new deployment and RPC at11689393 with zero lag and zero series. [Graph verification](evidence/operations/graph-upgrade-verification.json). New-image read-only startup passes protocol/proof checks but correctly blocks new sales without the keeper/bucket. No public sale or paid hosting occurred.

Hosted follow-up62e6b2b passed contracts and Substreams but its application job timed out during local reset/seed before any browser test ran. CI now compiles the complete local lifecycle before entering the120second setup helper; bounded setup diagnostics expose future failures. The upgrade runner adds4offline signature/journal regressions (13public-setup tests total). Final revised CI remains to be checked.

Final hosted verification: all three jobs passed for the complete source/deployment increment759e835: [run34698532030](https://github.com/MihRazvan/eth-online-2026/actions/runs/34698532030). This includes61hermetic browser regressions, both actual local-chain browser variants (including later replacement reorg), full financial lifecycle,51contracts,13public-setup tests,14keeper units and actual checkpoint/reorg,6operations,46settlement Node/13Python,29core/data/acquisition and Substreams checks. The production page serves the new manifest and passes disconnected desktop/mobile checks; [public smoke](evidence/operations/vercel-upgrade-smoke.json). Temporary local review services are stopped with their data retained. At11689438 the new deployment receipts remained canonical but were still beyond finalized head11689352; [observation](evidence/operations/upgrade-finality-observation.json). No hosted preservation or public sale acceptance is claimed.

Teammate handoff: [project and judge-demo brief](TEAMMATE_BRIEF.md) plus [exact frontend/local end-to-end runbook](DEMO_TEST_RUNBOOK.md) are available from README. Separate frontend and economic reviews checked the stated actions, rights and staged examples; local links were validated against the repository. Read-only production inspection at14:22UTC confirmed0activatedseries and operations503, so public rehearsal prerequisites are explicitly unfinished. This increment changes documentation only; linked guides now distinguish implemented keeper/Graph access from unoperated hosting and live composition.

Teammate-readiness implementation,12September: the public frontend identifies project-owned service pauses separately from wallet prerequisites, with a signature-free recheck and truthful analysis request/finality states. The bounded read-only Graph API and optional continuous history child are integrated into the operations image. A verified pre-sale Swap anchor avoids a4.26million-block lifetime replay; the retained11,299-block snapshot matches RPC/Studio at11690099. A temporary container advanced another95finalized blocks with public read-only proof retention healthy at the sampled checks, then stopped cleanly with11,394blocks retained and its writer lock released. This is local operation, not hosted acceptance or a live instrument join. [Implementation, review and evidence](evidence/graph-service.md).

Local verification passes54core/data/acquisition/stream/verifier tests,9operations tests,65hermetic browser tests, TypeScript/public build, both actual-chain browser lifecycle variants and actual bid/ask/cancellation with unchanged payouts/reserves. The quote regression now awaits rendered state after transaction refresh instead of racing it. Both replacement deployment receipts are independently observed finalized; [readback](evidence/operations/upgrade-finality-observation.json). Paid hosting remains unprovisioned pending the concrete budget decision; the dedicated keeper is not enabled. No public sale, joined series analysis or participant acceptance occurred. Lead owns implementation/operations/evidence; teammates perform the prepared wallet rehearsal once service readiness passes.

Final hosted verification for source8f4d962 passed all three jobs: [run34707231466](https://github.com/MihRazvan/eth-online-2026/actions/runs/34707231466). This includes contracts/static/mutation checks, Substreams,65UI regressions, both actual financial browser variants, the complete local lifecycle and actual maker bid/cancellation regression. Temporary review containers, Vite and Anvil are stopped; their private retained evidence stays outside Git. Public preservation and participant acceptance remain open.

Follow-up to the participant unable to sign and pin: fresh public `/api/operations` read still returns503 OPERATIONS_NOT_AVAILABLE. NFT approval and acceptance intentionally remain paused. Added the reason beside both disabled buttons with accessible descriptions, and clarified that approval does not publish a listing: the current flow requires a separate buyer-funded offer. Four targeted readiness/analysis browser regressions and the isolated public TypeScript/build pass; mobile layout reviewed. This wording change does not activate hosting, remove a financial gate or establish public acceptance.
