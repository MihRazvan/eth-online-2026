# Browser acceptance evidence

2026-09-11. The frontend specialist implemented and exercised the direct wallet/RPC adapter. The integration lead independently reran the saved transaction suite against the integrated application and a reset dedicated Anvil node.

- 14 deterministic fixture/scope tests pass: disconnected and wrong-network states, precision, rejected signatures/reverts, missing/expired quotes, insufficient funds, source lag, empty positions, narrow layout, keyboard focus, native ETH metadata and public deployment scope. Fixture transactions are labeled simulations.
- The separate real-chain browser test passes from root with `pnpm test:browser:chain`. It funds101test USDC, separately approves and accepts the original NFT, verifies8,000/10,000 buyer claims, publishes an Aqua strategy, trades1,000claims for10test USDC, captures after N, returns the same NFT while allocation is unresolved, submits the authentic retained header/storage witness and independently redeems all three holders plus residual assets. The unconsumed seed offer remains segregated. Every recorded action receipt is checked successful and actual balances are asserted.
- The lead's final rerun passed in8.2seconds, including seed/reset. `browser-chain-lifecycle.json` contains that run's action receipts, exact allocation, payout sum and segregated dust. Intermediate approval/checkpoint transactions can occur within an action; the action receipt list is not a complete block explorer export. Generated trace/screenshots live under ignored `apps/web/test-results`; representative retained screenshots are in `docs/design/evidence`.

The first root rerun reached actual proof allocation but timed out at the next wallet login. Same-URL/hash navigation had preserved the existing connection; the helper incorrectly waited for an absent Connect button. The correction connects only when necessary and asserts the requested actor's wallet identity. No payout or lifecycle assertion was removed or weakened.

The lead separately inspected the real local screenshots. Corrections include native-USDC wording, exact activation/end blocks, native ETH metadata, canonical Sepolia configuration checks, moving the technical growth baseline into the exact-terms disclosure, and suppressing net purchase scenarios when the price is unavailable. Historical bars appear only in labeled fixtures; a missing live Graph provider produces an explicit unavailable state. Sourced analysis binds a common block and persisted identity and displays coverage/finality separately.

These runs use local faucet currencies and explicitly enabled unlocked Anvil actors. They do not verify production wallet signing screens, public FeeStrip deployment, live Graph access or sponsor eligibility. The independent native-N collection comparison and actual full proof gas are separate evidence in `local-lifecycle.md` and `proof.md`.

Hosted fresh-checkout verification also passed for implementation `6ce74e1`: [GitHub Actions run34609205562](https://github.com/MihRazvan/eth-online-2026/actions/runs/34609205562). All three jobs completed successfully; the earlier failed run retained the same login-helper failure described above.
