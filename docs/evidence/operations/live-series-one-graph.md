# Live series 1: joined Graph evidence

At **2026-09-13 13:54:47 UTC**, the public buyer-analysis endpoint returned actual Sepolia series 1 joined with the hosted authenticated Substreams history and Studio deployment at finalized block **11696247**, hash `0xa81c275bfbb0c454d480f0240c27f4687c7b5c2424dccd647590a5739239d868`. This is a live nonempty-series readback. The [sanitized evidence](live-series-one-graph.json) records the response, source identities and independent canonical checks.

Studio and an independent PublicNode read agreed on NFT **39216**, PositionManager, PoolManager, USDC/WETH pool, claim token, range **58980–60180**, liquidity **3411586204**, activation **11696226**, endpoint **11696317**, and original supply **10000e18**. The activation receipt and zero activation fee-growth baseline were checked onchain. The NFT remained in FeeStrip custody at the selected source block. The original mint's zero-address Transfer was checked through the configured archive RPC and its block hash independently matched PublicNode. This distinguishes the actual current NFT and sale baseline from the older pool-history seed.

A read-only snapshot of the production SQLite store contained **17,447 consecutive blocks**, from **11678801** through **11696247**, the expected selected-pool/package identity, a retained provider cursor, and the configured historical tick anchor. The committed Substreams package SHA-256 matched the saved identity. All 20 selected tail hashes and the anchor hash matched independent RPC reads. Studio served the exact retained hash with the expected deployment and no indexing errors.

The series interval observed so far covers **22 of 22 known blocks**, all in range: **100% coverage and 100% block-weighted occupancy**. Recomputing this from the retained end-state tick observations independently matched the API. The API had **zero indexing lag** relative to finalized history; its 66-block distance from chain head was finality lag. This does not claim complete endpoint coverage or within-block fee attribution.

The request uses **1000e18 claims / 0.05 USDC / zero execution-cost assumption** as an underwriting input. Its **0.50 USDC whole-period break-even** follows the unchanged original denominator. These request values do not assert any wallet's current balance, executable ask inventory, actual acquisition cost or future income. Participant transfers and redemptions require separate current receipt/balance reconciliation.

Graph supplies historical context only. Donations and other controlled test activity can affect observations; no organic demand or forecast is established here. Allocation remains contract-only. Authenticated endpoint restoration, payouts and human wallet acceptance are separate evidence gates.
