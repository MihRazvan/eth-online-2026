# AI assistance and actual team involvement

The supplied feestrip-handoff/** packet is AI-assisted research and requirements material. The human user selected it as the implementation brief, authorized routine architecture decisions, specialist agents, verification, commits and pushes on 2026-09-11.

Initial README.md, AGENTS.md, docs/** and scripts/check-handoff.mjs: generated by Codex lead. Subsequent components will be recorded as integrated. Git uses the user's configured identity; commit trailers and this ownership ledger record AI assistance. No human code review or narration is claimed.

Human product review remains pending. Submission and video materials are historical; the user explicitly deprioritized hackathon work. Private system instructions, hidden reasoning and credentials are excluded.

## Implementation ownership

- contracts/src/FeeStrip.sol, FeeClaim.sol, interfaces, demo, contracts/test native lifecycle/invariants, contracts/script, protocol evidence: Codex protocol specialist. Canonical upstream sources were vendored unchanged at pins in contracts/dependencies.json; local demo faucets are explicitly fixtures.
- contracts/src/proof, proof tests and scripts, public witness acquisition/evidence: Codex proof specialist. Polytope code remains third-party Apache-2.0. Lead unified root testing/compiler pins.
- contracts/src/market and market tests; packages/core, packages/data, packages/subgraph; scripts/chain and generated ABI exports: Codex lead. Market instructions/runtime are derived from licensed official SwapVM sources with notices retained.
- apps/web and docs/design: Codex frontend specialist; three real rendered directions, references and critiques retained. Lead performed separate browser and concept review, identifying the corrected native-USDC wording and activation-block labels.
- Independent market reviewer identified callback-order stale-state bug. Its follow-up service run ended before a full report. Lead fixed and regression-tested four order/callback combinations; proof specialist independently confirmed the fix and reviewed protocol/market (not its own proof code), recorded in docs/evidence/integration-review.md.

No human implementation review, commercial validation, sponsor submission or narrated video has been recorded yet.

The lead authored CI, static-finding dispositions, the disposable mutation runner update, public deployment preflight, live buyer-query/API composition and reproducible development commands. The protocol specialist added the dynamic lifecycle/custody regressions and current-source sponsor feedback/submission packet. The proof specialist is implementing the reusable imported Substreams package, WASM execution tests and cursor sink. The frontend specialist is adding the real RPC/wallet adapter, Aqua quote publication, sourced-analysis view and saved browser transaction regression. Their verification scope is recorded separately from missing live access and human review.

On2026-09-11 the human user explicitly prioritized the actual product over hackathon requirements and requested broad research while gathering end-to-end test access. Codex product, infrastructure and engineering specialists authored the three supporting research memos; the lead wrote the synthesis and prioritized backlog. The lead implemented funded-offer recovery, witness-file metadata compatibility, isolated browser artifacts and regressions. The proof specialist hardened witness acquisition and added mock-RPC tests; the infrastructure specialist independently reviewed it and exercised a read-only public acquisition. The frontend specialist independently found the fixture's newest-offer cancellation bug, misleading expired-offer aggregate label and shared screenshot-directory collision. The lead fixed these and reran integrated verification. No human user-test outcome, audit or commercial validation is claimed.

The subsequent product/recovery increment was implemented by the same native team. The frontend specialist authored entitlement receipts, maker quote history/cancellation, actual bid execution, recovery UI/downloads and the directly verified cache allocation path. The proof specialist authored independent Python MPT and Node ABI-envelope authentication, then independently reviewed frontend scope, integer rounding, canonical quote recognition and signer continuity. The infrastructure specialist independently reviewed retention/reorg/durability/API behavior and authored the human user-test plan. The lead authored the SQLite retention worker, API, local configuration, fault tests and full real-service browser/cache lifecycle integration, corrected review findings, inspected rendered evidence, ran integrated tests, updated CI and pushed the increments. The lead and infrastructure reviewer independently checked the genuine Anvil zero-leaf compatibility correction. A signer-switch race, incoherent orphan handling, lifecycle-vs-endpoint finality, byte-digest mismatch, database repair and maintenance scheduling issues were found and corrected before integration. Separate review is not a security audit, and local unlocked-wallet transactions are not public wallet acceptance. No third-party skill package was installed in this increment.

For public setup, the lead authored scripts/public/deploy.mjs, rpc-proxy.mjs and rehearse-custody.mjs, integrated CI and ran the actual canonical fork plus browser checks. The proof specialist authored position.mjs and its nine offline tests, and reviewed public transaction bindings. The infrastructure specialist independently identified journal durability/exclusivity, resumed-envelope validation, rehearsal scope and saved-plan bounds issues; these were corrected before broadcast. The frontend specialist fixed missing/burned NFT discovery and public diagnostic redaction, separately reviewed by the lead and infrastructure specialist. The human user supplied project RPC access and funded the Sepolia wallet with test ETH and20testUSDC. No human acceptance session has been recorded.

The lead broadcast the reviewed12transaction Sepolia setup, verified NFT/balances separately, authored the private public-retention configuration helper and corrected its file-permission review finding. The proof specialist independently checked all6 public runtime hashes and15 immutable bindings using PublicNode. The lead inspected actual public-data desktop/mobile renders, corrected the empty-public-market guidance, started the read-only recovery service and retained scoped evidence. No injected-wallet participant signature or public fee sale/settlement is claimed.
