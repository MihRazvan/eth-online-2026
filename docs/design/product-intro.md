# Opening screen and product-facing status

The supplied [HTML reference](reference/usufruct-ui.html) supplies the centered citrus, crop marks, rotating vocabulary, 34-cell blue bar, type, spacing and timing. The bar measures this short introduction only; it never represents network loading, wallet approval or a completed transaction. Invented reference accession IDs and tick values are omitted.

The opening appears once per browser session, before manifest and RPC reads. Skip, Enter or Escape dismiss it; Tab stays on the skip control. Reduced motion uses a static half-second introduction. Existing hash links bypass it, and a new deep link dismisses it. The same wrapper survives bootstrap success or error, avoiding a second intro. A session flag is disclosed in Privacy.

Normal product screens no longer show successful service checks, service-component explanations, source-block counters or the public network/data diagnostic dump. A compact actionable pause still explains who must resolve availability. Adapter readiness checks, signing protections, exact earning endpoints, original-Q rights, receipt provenance and recovery information remain intact. Fixture demo controls remain explicitly labelled and opt-in.

Validation: 87 hermetic browser regressions passed. After final focus/copy refinements, all 10 intro and service-guidance checks passed again; TypeScript and the production build passed. These are browser/simulated-service results, not public wallet or settlement acceptance.

Rendered evidence:

- [Dark desktop intro](evidence/product-intro-dark-desktop.png)
- [Light mobile intro](evidence/product-intro-light-mobile.png)
- [Mobile pause and funded-sale controls](evidence/product-service-pause-mobile.png) — explicitly simulated services; safety gates remain disabled while paused.
