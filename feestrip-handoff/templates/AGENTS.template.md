# Repository guidance template

Adapt this into the actual host's repository instruction file after discovering the stack. Preserve existing instructions. Replace this introduction with concise project guidance; do not install it as a second competing instruction source.

## Product

FeeStrip sells a fixed period of an existing Uniswap v4 NFT's native USDC fees. The same NFT returns. Read `feestrip-handoff/PRODUCT_CONTRACT.md` for economic requirements before changing custody, accounting or settlement.

- Active range and liquidity are fixed.
- Claim transfers carry all unpaid sold-period accrual; preserve original-Q accounting.
- Capture before NFT return; exact N entitlement must remain valid through delayed proof.
- Reserves cannot fund trades or another series. Graph data cannot authorize payout.
- Required acceptance tests cannot be skipped or weakened to pass.

## Commands and maps

After inspecting the repository, record the actual setup, dev, seed/reset, fast verification and integration commands here. Link the real status ledger, ABI/interface map, ADR index and evidence manifest. Do not leave invented runnable commands or unresolved placeholders in the adopted file.

## Collaboration

One lead owns shared interfaces, root configuration, lockfiles and integration pushes. Give parallel writers separate worktrees and external resources. Reviewers inspect actual code or the running app. Keep the status/evidence checkpoint updated before committing.

## Git and attribution

Make small coherent commits and push using existing authorization. Preserve teammate work, branch rules, truthful identity/timestamps and AI attribution. No force-push, fabricated history or committed credentials.

Load detailed task-specific skills and references on demand. Keep this file short and specific to non-obvious repository practices.
